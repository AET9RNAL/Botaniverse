package com.aeternal.botaniverse.client.tesr;

public class DynamicManaLevel {
    /*int poolCapacity = Config.getPoolCapacityNilfheim();
    int currentManaLevel = ...


    int halfCapacityThreshold = poolCapacity / 2;
    int fullCapacityThreshold = poolCapacity;


        if (currentManaLevel >= halfCapacityThreshold && currentManaLevel < fullCapacityThreshold) {

        manaWater = com.aeternal.botaniverse.client.core.helper.IconHelper.forName(evt.getMap(), "mana_water2", "blocks");
    } else if (currentManaLevel >= fullCapacityThreshold) {

        manaWater = com.aeternal.botaniverse.client.core.helper.IconHelper.forName(evt.getMap(), "mana_water3", "blocks");
    }
}*/
}
